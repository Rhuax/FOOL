class a (int x)
{
    int getx() x;
}

class b implements a (int y)
{
    int gety()
        let
            int z = this.getx();
        in
            if (z>0) then {z} else {2};
}

;
let a og=new a(99); in 3;
